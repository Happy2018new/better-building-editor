# 控件与页面性能记录

2026-09-18，Windows 开发引擎 1.21.120，1920 × 1080 游戏客户区，游戏内 Tracy 10 秒采样。使用 `tools/profile_controls.py` 连续进行原生鼠标操作，动态效果开启，采样期间不发送剪贴板调试请求。帧率为采样总帧数 / 10 秒，包含操作间隔，不代表最低帧率或所有机器上的保证。

| 负载 | 修改前 | 第一轮 | 压缩控件后 | 外观与时序修正后 |
| --- | ---: | ---: | ---: | ---: |
| 连续拖动厚度滑条 | 14.3 FPS | 51.5 FPS | 57.5 FPS | 60.2 FPS |
| 选取 / 浏览切换 | 39.3 FPS | 34.4 FPS | 51.1 FPS | 52.9 FPS |
| 建筑库 / 工作台切换 | 29.9 FPS | 42.5 FPS | 51.4 FPS | 53.3 FPS |

原始采样 ID：

- baseline: `cap-20260918-112817-189364`, `cap-20260918-112948-b426c1`, `cap-20260918-113030-165d4c`
- compact: `cap-20260918-115853-562386`, `cap-20260918-115918-e846ee`, `cap-20260918-115940-6b7c82`
- final: `cap-20260918-122620-6de11c`, `cap-20260918-122701-d0dfd6`, `cap-20260918-122747-975f14`

改动：滑条本地更新读数和原生轨道，业务值即时写入、页面发布合并；页面移动容器并淡出单个覆盖层；纯视觉提交无需整屏刷新；相同 Element 的干净子树跳过递归更新；九宫圆角压缩成一个布局节点，粒子采用一张序列图片；工具参数保留稳定槽位，减少选项切换时重建。第一次粒子实现增加了布局节点并使选项切换回退，因此已被替换。

阶段验证：42 个 Python 单元测试、19 项游戏内编辑/滑条/拾取回归通过。外观与动画修正后已重新运行相同负载，结果见 final 列。

## 外观与动画验证

- 原生模型启用 scissor 时，分数裁剪边界会在右侧/底侧留下不受弹窗遮罩影响的白线。保留 scissor，只把独立模型裁剪框收缩到整数 UI 边界，并反向平移模型容器以维持原点；最大 300% 缩放仍正确裁剪。普通/专注视图及四种比例共 52 项适配、命中、裁剪边界检查通过。
- 输入框关闭原版背景，用固定圆角背景包裹原生编辑控件；使用原生已有内边距，避免窄附加值框重复缩进后裁掉数字。悬停/按下改为给同一圆角面着色，消除拉伸的椭圆。
- 分段背景在父页面提交完成后启动 300 ms 位移，避免页面重排吃掉动画前半程。原生坐标采样为 `2.53 → 6.68 → 27.74 → 70.41 → 71.69`；快速反向切换后精确归位。
- 确认弹窗保留退出阶段，遮罩渐变、卡片缩放和平移同时播放；按钮在退出时失效，Modal 继续拦截底层点击。进入、退出、反向切换等 6 项检查见 `tools/verify_motion.py`。
- `verify_motion.py` 沿用 seq 调试协议，缩短外部轮询间隔；标准 CLI 单次等待约 150 ms，不适合观察 300 ms 动画的完整过程。
- 原生鼠标 6 项检查通过。测试发现外部音乐浮窗可能遮住滑条，即使前台窗口仍为 Minecraft；性能脚本新增实际命中窗口校验，避免把点击其他浮窗当成有效 UI 负载。
- 专注编辑的 8 项回归通过，放大后视口面积保持 2.31 倍。最新连续放置采样约 3 秒 / 94 张画面，四次真实放置成功，未发现模型消失样本（基准覆盖 6113 像素，最低 6111）；这不等同于采集每个输出帧或覆盖任意规模建筑。
- 原有 16 项 UI 流程回归通过，包含输入配置名、保存/载入/删除、确认框、图层、投影创建与移除。业务脚本 MCDK 检查 13 个文件全部解析成功，保留函数长度、参数数量和既有 sys 导入的静态建议。

复现：

```powershell
python -X utf8 tools/profile_controls.py slider label
python -X utf8 tools/profile_controls.py segments label
python -X utf8 tools/profile_controls.py pages label
```

## 2026-09-18 全页反馈与缩放修复回归

继续使用 1920×1080、庭院示例、10 秒 Tracy 捕获和同一套实际鼠标负载，开启动画及全页点击特效：

| 负载 | 上阶段 final | 本轮 polish | 捕获 ID |
| --- | ---: | ---: | --- |
| 拖横滑条 | 60.2 FPS | 60.1 FPS | `cap-20260918-134008-332014` |
| 浏览 / 选取 | 52.9 FPS | 51.2 FPS | `cap-20260918-133923-2c4851` |
| 工作台 / 建筑库 | 53.3 FPS | 51.5 FPS | `cap-20260918-134034-557b77` |

帧数分别为 601、512、515；这是整个捕获窗口的平均值，不能说明每一帧都稳定。页面/选项切换仍有刷新开销，本轮没有声称提升这两项帧率，新增反馈后保持在约 51 FPS。不同运行时间的后台负载也会影响结果。

按钮不再各自持有粒子控件，页面统一复用 6 个 Image。点击只写原生位置、可见性和贴图，不调 session.emit、不新增 UI 节点。主题订阅仅在 scale/motion 改变时触发，平时仍保留组件复用和原生 transform 提交路径。
