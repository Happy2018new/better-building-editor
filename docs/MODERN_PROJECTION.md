# 现代化投影 · 使用说明

这是面向网易我的世界基岩版 ModSDK 的建筑编辑工作台。当前版本为可运行的开发预览版；暂不注册选区物品。

## 开始使用

1. 进入开发世界会自动打开工作台，并显示可以编辑的庭院示例。
2. 关闭工作台后，按 **P** 重新打开。对准方块按 **F6** 标记起点，按 **F7** 标记终点；没有瞄准方块时使用脚下坐标。两点之间显示蓝色线框。
3. 再次打开工作台，点击 **读取选区** 并确认，将世界方块读取到本地草稿。
4. 左侧选工具，右侧的 **当前选区** 与三维 **框选** 使用同一份范围。先框选或展开 **坐标设置** 修改起终点，再设置主/副材质、方块条件和工具参数，点击 **执行**。所有草稿操作都限制在当前文档长方体内。
5. **建筑库**中可以修改名称、保存多个配置、载入、重命名、删除，或指定 X/Y/Z 尺寸新建空白草稿。
6. **投影**中设置世界原点、不透明度、图层和缺失过滤，点击 **更新 / 生成投影**。关闭工作台即可在世界中照着投影搭建。配置保存在本机全局库，切换世界仍可使用。

## 工作台

- 点击场景右上角 **展开视图** 进入专注编辑：收起工具箱、分类栏和页面页签，让场景使用大部分窗口。**还原视图** 返回完整工作台；相机角度、缩放、编辑模式和草稿都会保留。
- 专注编辑中仍可保存、撤销/重做、切换完整/切面/单层、直接编辑和调整 Y 高度；点击 **材质与属性** 可随时展开右侧参数、图层与历史面板，再次点击收起。场景标题下显示当前主材质。
- 三维视图使用原生方块几何体。拖动连续控制水平和俯仰角，松手有短暂惯性；左右转、正视、俯视、复位和缩放会平滑过渡。鼠标停在视图内时，滚轮可以缩放；最小为 25%，取消原来的 300% 放大上限。视图左下的 **左移 / 右移 / 上移 / 下移** 按当前画面方向移动观察位置，建筑相应朝反方向移动。六向导航与切面均有文字和图标，窄视图自动分为两行。
- **前移 / 后移** 平滑拉近或拉远模型。在正交视图中通过缩放表达距离变化，和滚轮使用同一套平滑相机；不隐藏方块、不重建几何体。后移最小为 25%，前移无预设放大上限。查看内部使用场景上方的切面或单层。带房屋图标的 **复位** 按钮恢复角度、缩放和平移。
- 场景上方统一提供 **完整 / 切面 / 单层**，始终使用同一个三维视口。完整显示所有可见层；切面显示 Y 高度和下方；单层只显示 Y 高度。右侧的 **Y 输入框和 − / +** 可直接移动观察高度，底部 **工作层 Y** 与它同步。完整模式下 Y 只移动工作网格。比如石英建筑底部在 Y=1，网格也设为 Y=1 时才与其底边共面；Y=0 的网格在建筑下方一格。
- 大草稿总览保留真实方块坐标、材质与孔洞；移除了将每个 16³ 分块合成一个方块的旧预览。总览可直接操作，**精细** 按钮进入最多 32³ 的编辑范围，**总览** 返回完整建筑。参数中的 **精细视图中心 X, Y, Z** 可定位远处和地下，单格点击不会自动移动中心。
- 放置和删除只重新计算受影响分块的可见表面，再合并到同一个原生模型中绘制，使玻璃与墙体使用一致的遮挡关系。连续编辑合并模型提交，双缓冲在上传时保留已有画面；每次放置仍单独保留撤销。统计与历史在停止连续操作后合并刷新。
- 三维视图下方提供 **浏览、选取、放置、换材质、擦除、吸管、框选**。浏览点击定位单格；选取选择单格；换材质只替换点击位置的方块，不新增相邻方块；吸管读取主材质；框选依次点击两个角点。仅维护一份正式选区和一套 12 条框线，不会同时保留大框与小框。
- **放置** 时，鼠标悬停先用蓝框预览点击后要新增的格子；越界、锁层、已有方块或条件不允许时显示红框。悬停只改变框线预览，不修改正式选区或草稿。点击成功后选中新方块，鼠标不动时框留在刚放好的位置，移动鼠标后继续预览下一个目标。这样点击前能明确知道落点，点击后也不会立即跳到尚未放置的下一格。
- **触控放置**：轻触选择目标，抬手保留预览，再点 **确认放置**；**取消** 放弃候选。拖动仍然旋转视图，切换模式会取消候选。触屏事件会自动启用这种方式，PC 也可手动选择。
- **图层 → 场景亮度** 调整预览的明暗，不重新生成模型。三维预览使用固定光照，不随世界时间变暗；这个控件调整整个三维场景，框线和工具文字保持清楚。
- 输入框继续使用原生字体和整数 GUI 字形倍率。正在输入时仅该框变为中性深灰和浅色文字，使原生白光标清楚显示；取消焦点后恢复浅底深字。超长混排文本滚动到末尾仍有原生光标裁剪限制，详见 `INPUT_FONT_BATCHING.md`。
- **擦除** 的右栏提供 **单格 / 选区**。当前选中多格时，进入擦除自动选择“选区”，点击模型会保留原范围，点击右栏底部 **擦除选区** 一次删除范围内符合条件的方块。锁层和材质过滤继续生效，整次操作只需一次撤销，大范围可取消。选择“单格”后恢复逐块点击擦除，框线标记擦除后留下的空气格。
- 所有模式下拖动都用于旋转，超过移动阈值后不会同时编辑。松开鼠标后停止跟踪，保留短暂旋转惯性；拖出视口或切换页面会取消拖拽。放置模式点击空白处时，落点位于当前 Y 层；正视时射线与图层平行，可以切到俯视操作。
- 形状、纹理、修整使用当前蓝框选区；直接编辑更新选区到操作格，共用方块条件、图层锁与撤销历史。右侧 **全选** 恢复完整范围，坐标修改立即生效。框选第一点立即替换旧选区为一个格子，第二点扩展成长方体；第三点即为下一次框选的新起点。未确认终点时不能执行批量工具，切换模式取消未完成的框选并保留当前单格；空白处可以在当前 Y 层框选。
- **全部 / 方块 / 空气 / 材质** 过滤本次操作的目标位置；**方块** 表示已有的非空气方块，不表示生物实体。**材质** 使用单独的 **匹配材质**，与替换工具的 **替换来源** 相互独立。直接放置可跨出旧选区，仍受文档边界、图层可见性、图层锁和方块条件约束。
- **框选** 统一从视图下方进入；右侧集中提供 **全选 / 坐标设置** 与边界调整。下方 X、Y、Z 的 **下界 / 上界** 各有 − / + 按钮，每次移动一个方块，允许把空气位置纳入选区；自动限制在文档内，并防止上下界交错。它们与坐标输入和三维框选操作同一份选区。
- **网格** 位于当前 **工作层 Y** 的底面，每格都绘制边界线；64×64 平面具有每轴 65 条边界线，不抽样。使用底部 Y 的 − / + 明确调整高度，点击选取不会移动它。网格不改变选区或方块；非长方体选择的唯一蓝框显示其包围盒。
- 右侧只展示当前操作使用的参数：例如空心形状显示厚度、移动显示步长、随机混合显示比例和种子。横滑条留有端点空间，数值会按整数吸附；竖滚动条按内容长度显示，可点击轨道或拖动，也支持原生滚轮滚动。切换工具后参数面板回到顶部。
- 底部 Y 控件切换当前层；图层面板每页 16 层，支持翻页、锁定、隐藏和隔离。锁定保护编辑，隐藏只改变预览。
- 历史记录支持撤销/重做。状态栏显示当前方块、选区和材质数量。
- 工具箱支持中文名称、说明及英文工具 ID 搜索。
- 默认使用高清 Noto Sans SC 离线文字资源，未知字符回退原生 smooth 字体。
- 输入框保留原生字体，按游戏 GUI 倍率调整字号；当前常见窗口下使用 3 倍物理放大，比上一版的 2 倍大 50%，同时保留整数像素缩放。输入文字没有换成黑体，也没有新增文字图片。
- 新建尺寸支持英文或中文逗号、全角数字、空格以及 `×`、`*`、`x` 分隔。例如 `３，８，３`、`3×8×3` 都会创建 3×8×3。格式不完整时禁用新建，超出 **64×100×64** 时明确报错并保留当前草稿；载入和世界捕获采用同一限制，旧的超限配置不自动截断。
- 按钮有弹性反馈，操作按钮带点击粒子，滑块有回弹，分段选项有移动背景，页面有平移淡入；入门指南提供减少动态效果开关。

## 投影和世界写入

投影是本地客户端的透明几何体，不修改真实方块，也不消耗材料。修改投影原点、图层、透明度或缺失过滤后，点击更新。建造进度通过服务端重新检查，区分已完成、缺失和材质不符。

大范围投影随玩家移动加载附近的精确 16³ 分块，同时保留最近的最多 32 个分块。远处方块仍保存在配置里，走近后显示。编辑器总览和世界投影都使用真实方块坐标与材质。

创造模式可将草稿应用到世界。默认只放置非空气方块；开启 **同步空气** 后，会同步整个长方体并清除草稿中空气对应的位置，提交前再次确认。服务端使用引擎提供的客户端身份，检查游戏模式、维度、距离、体积、加载状态及受保护的方块实体，并按批次执行。

**撤销世界写入**只撤销最近一次已完成的世界操作，跳过已被其他玩家改动的方块。写入失败时尝试回滚；如果区块卸载使恢复失败，会保留剩余恢复记录并明确提示。测试世界的 27 格写入/撤销已通过实际游戏验证。

## 当前范围与边界

- X/Z 各 1–64 格，Y 为 1–100 格，最多 **409,600 格**；本机建筑库最多 32 个配置。建筑库的 **新建最大区域** 可直接创建 64×100×64 草稿；原有超限配置保留在库中，载入时明确拒绝，不截断或删除。
- 大范围工具按时间片运行，状态栏显示进度，右侧可 **取消编辑**；完成前草稿保持原状。填充等均匀分块操作有快速路径，逐格形状、随机纹理、连通填充等耗时会随体积增长。
- 草稿历史最多 50 步，按约 64 MiB 估算预算淘汰旧记录，至少保留最近一次。大配置分批保存与载入，旧版小配置仍可使用。
- 世界读取、进度检查和写入通过分包和分批处理，可 **取消世界操作**；写入中取消会尝试回滚。世界操作须访问大量原生方块 API，耗时与草稿编辑不同。实际世界写入/撤销验证为 27 格，本轮没有进行最大范围的世界写入。
- 配置保存方块名称和附加值，不包含箱子物品、告示牌文字、实体、结构 NBT 等内容；实际写入保护既有方块实体。
- 尚未实现各种方向性方块的完整状态变换，因此对楼梯、门等方块的旋转/镜像会明确拒绝。
- 水、火、传送门、部分自定义方块外观等受 SDK 方块几何体支持范围限制，可能不出现在三维模型中；方块数据仍保存在草稿里。
- 三维命中按方块格子的包围盒计算。楼梯、薄片等非完整立方体按所在格子选择；SDK 不显示的方块建议在逐层视图操作。当前支持逐次点击编辑，尚未提供按住鼠标连续涂抹。
- 编辑器三维模型采用固定光照，可在图层面板调整场景亮度。世界投影继续使用引擎世界光照。
- 世界写入保留当前版本 aux 值，并关闭邻居更新以限制批量操作的连锁影响。复杂红石、液体及跨版本方块状态需要进一步兼容测试。
- 建造进度检查和投影刷新由玩家主动触发；本轮未添加自动补方块、材料消耗、跨客户端共享库、结构文件导入导出或选区物品。
- 当前主要验证 PC 键鼠入口；触屏专用选区入口和完整移动端机型适配仍需后续工作。

## 64 个工具

| 分类 | 工具 | 用途 |
| --- | --- | --- |
| 选区 | 全选区域 | 选择整个建筑范围。 |
| 选区 | 选择方块 | 仅选择已有方块的位置。 |
| 选区 | 选择空气 | 仅选择空白位置。 |
| 选区 | 同类材质 | 选择与替换来源相同的方块。 |
| 选区 | 当前图层 | 选择当前 Y 层。 |
| 选区 | 反向选择 | 反转当前选择的格子。 |
| 选区 | 扩展选区 | 沿六个方向扩展一个方块。 |
| 选区 | 收缩选区 | 沿六个方向收缩一个方块。 |
| 选区 | 选择表面 | 选取与空气接触的方块表面。 |
| 选区 | 坐标框选 | 使用参数中的起点与终点框选。 |
| 编辑 | 填充方块 | 用主材质填满选区。 |
| 编辑 | 清空选区 | 删除选区内的方块，可以撤销。 |
| 编辑 | 替换材质 | 将来源材质替换为主材质。 |
| 编辑 | 仅填空气 | 保留已有建筑，只填空白。 |
| 编辑 | 表面涂装 | 为暴露的方块更换材质。 |
| 编辑 | 复制选区 | 复制相对坐标和材质到内部剪贴板。 |
| 编辑 | 剪切选区 | 复制并移除选区内方块。 |
| 编辑 | 粘贴方块 | 以参数起点为原点粘贴；越界时拒绝。 |
| 编辑 | 无空气粘贴 | 仅粘贴剪贴板中的非空气方块。 |
| 编辑 | 连通填充 | 从参数起点开始，填充六向连通的同类方块。 |
| 编辑 | 交换材质 | 交换主材质和来源材质。 |
| 变换 | 水平旋转 90° | 围绕选区中心旋转，保留块状材质属性。 |
| 变换 | 水平旋转 180° | 将选区水平转向相反方向。 |
| 变换 | 水平旋转 270° | 围绕选区中心逆向旋转。 |
| 变换 | 沿 X 镜像 | 左右翻转选区内的方块。 |
| 变换 | 沿 Y 镜像 | 上下翻转选区内的方块。 |
| 变换 | 沿 Z 镜像 | 前后翻转选区内的方块。 |
| 变换 | 向东移动 | 沿 X 正方向移动步长个方块。 |
| 变换 | 向西移动 | 沿 X 负方向移动步长个方块。 |
| 变换 | 向上移动 | 沿 Y 正方向移动步长个方块。 |
| 变换 | 向下移动 | 沿 Y 负方向移动步长个方块。 |
| 变换 | 向南移动 | 沿 Z 正方向移动步长个方块。 |
| 变换 | 向北移动 | 沿 Z 负方向移动步长个方块。 |
| 变换 | 沿 X 阵列 | 按选区宽度复制一次；预先检查边界。 |
| 变换 | 沿 Z 阵列 | 按选区深度复制一次；预先检查边界。 |
| 形状 | 实心长方体 | 在选区内生成实心长方体。 |
| 形状 | 空心长方体 | 生成厚度可调的闭合外壳。 |
| 形状 | 四面围墙 | 仅生成四周墙体，保留顶和底。 |
| 形状 | 方框骨架 | 沿长方体十二条边生成骨架。 |
| 形状 | 实心椭球 | 按选区宽高深生成椭球。 |
| 形状 | 空心椭球 | 生成厚度可调的椭球壳。 |
| 形状 | 实心圆柱 | 沿 Y 轴生成椭圆截面的圆柱。 |
| 形状 | 空心圆柱 | 生成环形圆柱，可用于塔楼。 |
| 形状 | 金字塔 | 从底到顶收拢的四棱锥。 |
| 形状 | 半球穹顶 | 生成适配选区的上半椭球壳。 |
| 形状 | 圆拱门 | 沿 Z 方向生成圆拱与两侧立柱。 |
| 形状 | 阶梯结构 | 沿 X 方向生成逐级升高的实心楼梯。 |
| 形状 | 空间直线 | 连接参数起点与终点。 |
| 形状 | 铺设地板 | 铺满选区最底层。 |
| 形状 | 双坡屋顶 | 沿 X 方向生成对称的双坡顶。 |
| 纹理 | 棋盘拼色 | 用主副材质生成三维棋盘纹理。 |
| 纹理 | 纵向条纹 | 沿 X 方向按步长交替主副材质。 |
| 纹理 | 分层条纹 | 沿 Y 方向交替主副材质。 |
| 纹理 | 横向条纹 | 沿 Z 方向按步长交替主副材质。 |
| 纹理 | 随机混合 | 按混合比例与固定种子生成可复现纹理。 |
| 纹理 | 高度渐变 | 从底到顶逐渐增加副材质比例。 |
| 纹理 | 砖缝排列 | 错缝砖纹，步长控制砖块宽度。 |
| 纹理 | 网格镂空 | 生成主材质框架和副材质连接点。 |
| 修整 | 掏空内部 | 删除未与空气接触的内部方块。 |
| 修整 | 清理孤点 | 移除六个方向均为空气的孤立方块。 |
| 修整 | 修补孔洞 | 填充六个方向均为方块的单格孔洞。 |
| 修整 | 垂直压实 | 按列将方块从选区底部向上紧密排列。 |
| 修整 | 向下打基 | 将每列已有最低方块向下延伸到选区底。 |
| 修整 | 保留顶面 | 每列仅保留最高的方块。 |
